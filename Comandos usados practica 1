Practica 2: Configuración de parametros de red.

ip addr
#Configurar DHCP 
nmcli con add type ethernet con-name dhcp-connection ifname ens33 ipv4.method auto
nmcli con up dhcp-connection
nmcli con show dhcp-connection
#Configurar red estatica
nmcli con add type ethernet con-name static-connection ifname ens33 ipv4.method manual ipv4.address 192.168.1.100/24 ipv4.gateway 192.168.1.1 ipv4.dns "8.8.8.8 8.8.4.4"
nmcli con up static-connection
nmcli con show static-connection

Practica 3: Gestion de Usuario y grupos

# Crear usuario con nombre "usuario123"
sudo adduser usuario123
sudo passwd usuario123
# Agregar usuario al grupo wheel (permisos sudo)
sudo usermod -aG wheel usuario123
# Verificar grupos del usuario
groups usuario123
# Crear grupo "guest"
sudo groupadd guest
# Crear usuario "guestuser"
sudo adduser guestuser
sudo passwd guestuser
# Agregar "guestuser" al grupo "guest"
sudo usermod -aG guest guestuser
# Verificar grupos de guestuser
groups guestuser
# Eliminar usuario guestuser y su home
sudo userdel -r guestuser
# Eliminar grupo guest
sudo groupdel guest

Practica 4: Gestion de Permisos de archivos

# Crear carpeta materia
mkdir materia
cd materia
# Crear archivo estudiante.txt
touch estudiante.txt
# Editar con vi (manual)
vi estudiante.txt
# (En vi: i para insertar, escribir texto, Esc, :wq para guardar y salir)
# Cambiar permisos para control total solo propietario
chmod 700 estudiante.txt
# Cambiar permisos para control total solo grupo propietario
chmod 070 estudiante.txt
cd ..
# Crear carpeta materia2
mkdir materia2
# Copiar archivo estudiante.txt a materia2
cp materia/estudiante.txt materia2/
# Eliminar carpeta materia y su contenido
rm -r materia

